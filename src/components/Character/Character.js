// Displays Character Name, HP, and "view charaacter" button that opens modal view
// Modal View Displays class, race, stats, and CompletedStories Component
// Used by MyCharacters Component

import React, { Component } from 'react';

export default class Character extends Component {
    render () {
        return (
            
        );
    }
}