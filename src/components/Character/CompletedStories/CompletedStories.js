// Characters completed stories - Displays list of ViewStory Components
// Used by the Character Component

import React, { Component } from 'react';

export default class CompletedStories extends Component {
    render () {
        return (
            
        );
    }
}