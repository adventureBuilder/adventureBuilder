import React, { Component } from 'react';

export default class StorySelection extends Component {
    render () {
        return (
            <div>
            StorySelection
            </div>
        );
    }
}